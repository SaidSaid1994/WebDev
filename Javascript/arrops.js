function arrayReverse(arr)
{
     var newString = "";
 

    for (var i = arr.length - 1; i >= 0; i--)
	{ 
        newString += str[i]; 
    }
	alert("Reverse String : "+newString)
}

function arraySize(arr)
{
	var len=arr.length;
	alert("Size of Array : "+len);
}
